const searchInput=document.querySelector('#styleSearch');const categoryFilter=document.querySelector('#categoryFilter');const cards=[...document.querySelectorAll('.catalog-card')];const resultCount=document.querySelector('#resultCount');const emptyState=document.querySelector('#emptyState');function applyFilters(){const term=searchInput.value.trim().toLowerCase();const category=categoryFilter.value;let visible=0;cards.forEach(card=>{const matchesText=!term||card.dataset.name.includes(term);const matchesCategory=category==='all'||card.dataset.category===category;const show=matchesText&&matchesCategory;card.hidden=!show;if(show)visible++;});resultCount.textContent=`${visible} estilo${visible===1?'':'s'} visible${visible===1?'':'s'}`;emptyState.hidden=visible!==0;}searchInput.addEventListener('input',applyFilters);categoryFilter.addEventListener('change',applyFilters);applyFilters();